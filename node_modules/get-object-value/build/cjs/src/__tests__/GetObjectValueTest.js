"use strict";

var _interopRequireWildcard = require("reshow-runtime/helpers/interopRequireWildcard");

var _chai = require("chai");

var _index = _interopRequireWildcard(require("../index.js"));

describe('Test Get Object Value', function () {
  it('test get', function () {
    var a = {
      foo: {
        bar: {
          key: 'v'
        }
      }
    };
    var acture = (0, _index["default"])(a, ['foo', 'bar', 'key']);
    (0, _chai.expect)(acture).to.equal('v');
  });
  it('test toMap', function () {
    var map = {
      a: {
        toJS: function toJS() {
          return 'foo';
        }
      },
      b: {
        toJS: function toJS() {
          return 'bar';
        }
      }
    };
    (0, _chai.expect)((0, _index.toMap)(map)).to.deep.equal({
      a: 'foo',
      b: 'bar'
    });
  });
});