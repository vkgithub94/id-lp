import getSafeReg, { cacheReg } from 'get-safe-reg';

var getRegString = function getRegString(name) {
  return '(?:^|\\s+)' + getSafeReg(name) + '(?:\\s+|$)';
};

var cache = cacheReg({})(getRegString);

export default (function (name) {
  return cache(name);
});