import { win, doc } from '../index';
import { expect } from 'chai';
describe('Test doc', function () {
  it('test doc', function () {
    var d = doc();
    expect(d).to.equal(document);
  });
});
describe('Test win', function () {
  it('test win', function () {
    var w = win();
    expect(w).to.equal(window);
  });
});