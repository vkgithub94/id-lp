"use strict";

var _chai = require("chai");

var _queryFrom = require("../queryFrom");

describe('Test query ancestor', function () {
  after(function () {
    return document.body.innerHTML = '';
  });
  it('test closest and polyfill', function () {
    document.body.innerHTML = "\n<main id=\"root\"><div id=\"foo\"><span id=\"bar\"></span></div></main>\n";

    var el = _queryFrom.defaultQuery.one('#bar');

    var foo = _queryFrom.defaultQuery.ancestor(el, '#foo');

    el.closest = undefined;

    var foo1 = _queryFrom.defaultQuery.ancestor(el, '#foo');

    (0, _chai.expect)(foo.isSameNode(foo1)).to.be["true"];
  });
});