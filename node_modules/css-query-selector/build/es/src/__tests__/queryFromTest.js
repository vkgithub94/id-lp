import { expect } from 'chai';
import queryFrom from '../queryFrom';
describe('Test', function () {
  it('sample test', function () {
    var query = queryFrom(function () {
      return document;
    });
    var name = query.one('body').nodeName;
    expect(name).to.be.equal('BODY');
  });
});