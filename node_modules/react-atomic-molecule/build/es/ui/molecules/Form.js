import _extends from "reshow-runtime/es/helpers/extends";
import _objectWithoutProperties from "reshow-runtime/es/helpers/objectWithoutProperties";

/* jshint esnext: true */
import React from 'react';
import { mixClass } from 'class-lib';
import SemanticUI from '../molecules/SemanticUI';

var Form = function Form(props) {
  var messageType = props.messageType,
      className = props.className,
      others = _objectWithoutProperties(props, ["messageType", "className"]);

  var classes = mixClass(className, messageType, 'form');
  return React.createElement(SemanticUI, _extends({
    atom: "form"
  }, others, {
    className: classes
  }));
};

export default Form;